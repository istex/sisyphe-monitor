{
  "name": "sisyphe-monitor",
  "version": "2.0.3",
  "main": "index.js",
  "dependencies": {
    "bluebird": "^3.5.0",
    "bull": "^3.1.0",
    "redis": "^2.8.0",
    "request": "^2.81.0"
  },
  "scripts": {
    "package-linux": "electron-packager . --overwrite --platform=linux --arch=all --out=build",
    "package-mac": "electron-packager . --icon='logo-sisyphe.icns' --overwrite --platform=darwin --arch=all --out=build",
    "package-windows": "electron-packager . --icon='logo-sisyphe.ico' --overwrite --platform=win32 --arch=all --out=build"
  },
  "devDependencies": {
    "electron": "^1.7.6",
    "electron-packager": "^9.1.0"
  }
}
